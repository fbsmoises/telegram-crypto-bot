{
  "buildCommand": "pip install -r requirements.txt",
  "startCommand": "python bot.py",
  "environments": {
    "production": {
      "env": {
        "PYTHON_VERSION": "3.10",
        "PORT": "8080"
      }
    }
  }
}
